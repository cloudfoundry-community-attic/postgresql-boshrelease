---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - postgresql
  roles:
  - name: testuser
    password: testpassword
  databases:
  - name: testdb

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: postgresql
    version: latest
